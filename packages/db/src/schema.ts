// Schema definitions are split by domain under ./schemas. This file is the stable
// entry point — `@mcd/db` and `@mcd/db/schema` resolve here and re-export the lot.
export * from './schemas/index.ts';
