// Shared values used across hooks and pages. Keep this file small — only
// values that are referenced from more than one place, or that change the
// product behavior in a way reviewers should see at a glance.

export const PAGE_SIZE = 20;
export const SEARCH_DEBOUNCE_MS = 300;
export const SIMILAR_MOVIES_LIMIT = 10;
